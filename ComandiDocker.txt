
docker run --name postgres -e POSTGRES_USER=root -e POSTGRES_PASSWORD=root -e POSTGRES_DB=pawtropolis -p 5432:5432 library/postgres -d postgres

docker cp script.sql postgres:/script.sql

docker exec -it postgres psql -U root -d pawtropolis -f /script.sql
